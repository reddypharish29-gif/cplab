
#include<stdio.h>
int main ()
{
    int a,b;
    printf("entre two number:");
    scanf("%d%d",&a,&b);
    if(a>0)
    {
        if(b>0)
        {
            printf("both number are postive\n");
        }
    }
    return 0;
}

